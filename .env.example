# Copy to .env.local and fill in. Both are read server-side only (src/app/api/enquiry/route.ts).
# The publishable key is safe to expose, but RLS is what actually protects the table:
# anon may INSERT into public.enquiries and nothing else.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
